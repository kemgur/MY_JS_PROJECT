/*Error Object
The Error object provides error information when an error occurs.
Error nesnesi, bir hata oluştuğunda hata bilgisi sağlar.

Example
In this example we have written "alert" as "adddlert" to deliberately produce an error.
Bu örnekte, kasıtlı olarak bir hata üretmek için "uyarı" yı "adddlert" olarak yazdık.

Return the error name and a description of the error:
Hata adını ve hatanın açıklamasını verin:
*/

//I got a comment for giving an error message.
//Hata mesaji verdigi icin yoruma aldim.

/*
try {
    adddlert("Welcome");
  }
  catch(err) {
    document.getElementById("demo").innerHTML =
    err.name + "<br>" + err.message;

    */
