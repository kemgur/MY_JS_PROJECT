/*
Math Object:
The Math object allows you to perform mathematical tasks.
Math nesnesi, matematiksel görevleri gerçekleştirmenize izin verir.

Math is not a constructor. All properties/methods of Math can be called by using Math as an object, 
without creating it:
Math bir yapıcı değildir. Math'ın tüm özellikleri / yöntemleri, Math'ı bir nesne olarak,
 onu oluşturmadan kullanarak çağrılabilir:
*/

var x = Math.PI;            // Returns PI
var y = Math.sqrt(16);      // Returns the square root of 16

